name: Multi-Platform Builds

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-web:
    name: Build Web
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for web
        run: |
          if npm run | grep -q "build:web"; then npm run build:web; else echo "Skipping build:web (script missing)"; fi

      - name: Upload web build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: web-build
          path: dist/

  build-ios:
    name: Build iOS
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for iOS
        run: |
          if npm run | grep -q "build:ios"; then npm run build:ios; else echo "Skipping build:ios (script missing)"; fi

      - name: Upload iOS build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: ios-build
          path: dist-ios/

  build-macos:
    name: Build macOS
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for macOS
        run: |
          if npm run | grep -q "build:macos"; then npm run build:macos; else echo "Skipping build:macos (script missing)"; fi

      - name: Upload macOS build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: macos-build
          path: builds/macos/

  build-windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for Windows
        run: |
          if npm run | grep -q "build:windows"; then npm run build:windows; else echo "Skipping build:windows (script missing)"; fi

      - name: Upload Windows build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: windows-build
          path: builds/windows/

  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for Linux
        run: |
          if npm run | grep -q "build:linux"; then npm run build:linux; else echo "Skipping build:linux (script missing)"; fi

      - name: Upload Linux build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: linux-build
          path: builds/linux/

  build-winuwp:
    name: Build WinUWP
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for WinUWP
        run: |
          if npm run | grep -q "build:winuwp"; then npm run build:winuwp; else echo "Skipping build:winuwp (script missing)"; fi

      - name: Upload WinUWP build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: winuwp-build
          path: dist-winuwp/

  build-android:
    name: Build Android
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install dependencies
        run: npm ci

      - name: Build for Android
        run: |
          if npm run | grep -q "build:android"; then npm run build:android; else echo "Skipping build:android (script missing)"; fi

      - name: Upload Android build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: android-build
          path: platforms/android/build/

  build-tizen:
    name: Build Tizen
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Tizen CLI
        run: npm install -g tizen-cli

      - name: Install dependencies
        run: npm ci

      - name: Build for Tizen
        run: |
          if npm run | grep -q "build:tizen"; then npm run build:tizen; else echo "Skipping build:tizen (script missing)"; fi

      - name: Upload Tizen build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: tizen-build
          path: platforms/tizen/build/

  build-flutter:
    name: Build Flutter
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install dependencies
        run: npm ci

      - name: Build for Flutter
        run: |
          if npm run | grep -q "build:flutter"; then npm run build:flutter; else echo "Skipping build:flutter (script missing)"; fi

      - name: Upload Flutter build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: flutter-build
          path: platforms/flutter/build/

  build-tvos:
    name: Build tvOS
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for tvOS
        run: |
          if npm run | grep -q "build:tvos"; then npm run build:tvos; else echo "Skipping build:tvos (script missing)"; fi

      - name: Upload tvOS build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: tvos-build
          path: platforms/tvos/build/

  build-aurora:
    name: Build Aurora
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for Aurora
        run: |
          if npm run | grep -q "build:aurora"; then npm run build:aurora; else echo "Skipping build:aurora (script missing)"; fi

      - name: Upload Aurora build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: aurora-build
          path: dist-aurora/

  build-harmonyos:
    name: Build HarmonyOS
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for HarmonyOS
        run: |
          if npm run | grep -q "build:harmonyos"; then npm run build:harmonyos; else echo "Skipping build:harmonyos (script missing)"; fi

      - name: Upload HarmonyOS build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: harmonyos-build
          path: platforms/harmonyos/build/

  deploy-web:
    name: Deploy Web to GitHub Pages
    runs-on: ubuntu-latest
    needs: build-web
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Download web build artifacts
        uses: actions/download-artifact@v6
        with:
          name: web-build
          path: dist/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
